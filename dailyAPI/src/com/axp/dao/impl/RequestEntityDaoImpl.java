package com.axp.dao.impl;import java.util.List;import org.hibernate.Query;import org.hibernate.Session;import org.springframework.stereotype.Repository;import com.axp.dao.IRequestEntityDao;import com.axp.domain.RequestEntity;@Repositorypublic class RequestEntityDaoImpl extends BaseDaoImpl<RequestEntity> implements IRequestEntityDao {	@SuppressWarnings("unchecked")	@Override	public List<RequestEntity> getRequestEntityByRequestName(String requestName) {//根据请求的名称找寻请求的实体类		Session session = sessionFactory.getCurrentSession();		Query query = session.createQuery("from RequestEntity where invokeName=:name");		return query.setParameter("name", requestName).list();	}}